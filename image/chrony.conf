# image/chrony.conf

pool pool.ntp.org iburst

driftfile /var/lib/chrony/chrony.drift
rtcsync

# Allow any client to query this server for time.
# Override via the mounted production config to restrict this.
allow all
